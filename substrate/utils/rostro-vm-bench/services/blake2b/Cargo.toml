# SPDX-License-Identifier: Apache-2.0
# Copyright (C) Rostro Foundation
# Vendored from JAR/Grey services/benches/blake2b (Apache-2.0). Original
# copyright: JAR contributors. Used as a real-Rust crypto workload for the
# Rostro VM benchmark harness — compiles unchanged to javm + polkavm targets,
# with a wasm32 entry point added for wasmtime-side comparison.

[package]
name = "rostro-bench-blake2b"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Blake2b-256 hashing benchmark — compiled to javm/polkavm/wasm32 for VM comparison"
publish = false

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
blake2 = { version = "0.10", default-features = false }
javm-builtins = { package = "rostro-bench-javm-builtins", path = "../javm-builtins" }

[target.'cfg(target_env = "polkavm")'.dependencies]
polkavm-derive = "0.32.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_env, values("javm", "polkavm"))', 'cfg(target_os, values("none"))', 'cfg(target_arch, values("wasm32"))'] }

[workspace]
