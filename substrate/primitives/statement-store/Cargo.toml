[package]
name = "rp-statement-store"
version = "25.0.0"
authors.workspace = true
edition.workspace = true
license = "Apache-2.0"
homepage.workspace = true
repository.workspace = true
description = "A crate which contains primitives related to the statement store"
readme = "README.md"

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
codec = { features = ["derive"], workspace = true }
frame-support = { workspace = true }
scale-info = { features = ["derive"], workspace = true }
serde = { optional = true, features = ["alloc", "derive"], workspace = true }
rp-api = { workspace = true }
rp-application-crypto = { workspace = true }
rp-core = { workspace = true }
rp-crypto-hashing = { workspace = true }
rp-externalities = { workspace = true }
rp-runtime = { workspace = true }
rp-runtime-interface = { workspace = true }
thiserror = { optional = true, workspace = true }

# ECIES dependencies
aes-gcm = { optional = true, workspace = true }
curve25519-dalek = { optional = true, workspace = true }
ed25519-dalek = { optional = true, workspace = true, default-features = true }
hkdf = { optional = true, workspace = true }
rand = { features = ["small_rng"], optional = true, workspace = true, default-features = true }
sha2 = { optional = true, workspace = true, default-features = true }
x25519-dalek = { optional = true, features = ["static_secrets"], workspace = true }

[features]
default = ["std"]
std = [
	"aes-gcm",
	"aes-gcm?/std",
	"codec/std",
	"curve25519-dalek",
	"ed25519-dalek",
	"frame-support/std",
	"hkdf",
	"hkdf?/std",
	"rand",
	"scale-info/std",
	"serde?/std",
	"sha2",
	"rp-api/std",
	"rp-application-crypto/std",
	"rp-core/std",
	"rp-crypto-hashing/std",
	"rp-externalities/std",
	"rp-runtime-interface/std",
	"rp-runtime/std",
	"thiserror",
	"x25519-dalek",
]
serde = [
	"dep:serde",
	"scale-info/serde",
	"rp-application-crypto/serde",
	"rp-core/serde",
	"rp-runtime/serde",
]
