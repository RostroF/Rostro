[package]
name = "frame-system"
version = "46.0.0"
authors.workspace = true
edition.workspace = true
license = "Apache-2.0"
homepage.workspace = true
repository.workspace = true
description = "FRAME system module"
readme = "README.md"

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[[bench]]
name = "bench"
harness = false

[dependencies]
cfg-if = { workspace = true }
codec = { features = ["derive"], workspace = true }
docify = { workspace = true }
frame-support = { workspace = true }
log = { workspace = true }
scale-info = { features = ["derive", "serde"], workspace = true }
serde = { features = ["alloc", "derive"], workspace = true }
rp-core = { features = ["serde"], workspace = true }
rp-io = { workspace = true }
rp-runtime = { features = ["serde"], workspace = true }
rp-version = { features = ["serde"], workspace = true }
rp-weights = { features = ["serde"], workspace = true }

[dev-dependencies]
criterion = { workspace = true, default-features = true }
rp-externalities = { default-features = true, workspace = true }
rp-tracing = { default-features = true, workspace = true }
substrate-test-runtime-client = { workspace = true }

[features]
default = ["std"]
std = [
	"codec/std",
	"frame-support/std",
	"log/std",
	"scale-info/std",
	"serde/std",
	"rp-core/std",
	"rp-io/std",
	"rp-runtime/std",
	"rp-version/std",
	"rp-weights/std",
]
runtime-benchmarks = [
	"frame-support/runtime-benchmarks",
	"rp-runtime/runtime-benchmarks",
]
try-runtime = ["frame-support/try-runtime", "rp-runtime/try-runtime"]
experimental = ["frame-support/experimental"]
