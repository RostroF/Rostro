[package]
name = "frame-support"
version = "46.0.0"
authors.workspace = true
edition.workspace = true
license = "Apache-2.0"
homepage.workspace = true
repository.workspace = true
description = "Support code for the runtime."
readme = "README.md"

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
array-bytes = { workspace = true }
binary-merkle-tree = { workspace = true }
bitflags = { workspace = true }
codec = { features = ["derive", "max-encoded-len"], workspace = true }
docify = { workspace = true }
environmental = { workspace = true }
frame-metadata = { features = ["current", "unstable"], workspace = true }
frame-support-procedural = { workspace = true }
impl-trait-for-tuples = { workspace = true }
k256 = { features = ["ecdsa"], workspace = true }
log = { workspace = true }
macro_magic = { workspace = true }
paste = { workspace = true, default-features = true }
scale-info = { features = ["derive"], workspace = true }
serde = { features = ["alloc", "derive"], workspace = true }
serde_json = { features = ["alloc"], workspace = true }
rp-api = { features = ["frame-metadata"], workspace = true }
rp-arithmetic = { workspace = true }
rp-core = { workspace = true }
rp-crypto-hashing-proc-macro = { default-features = true, workspace = true }
rp-debug-derive = { workspace = true }
rp-genesis-builder = { workspace = true }
rp-inherents = { workspace = true }
rp-io = { workspace = true }
rp-metadata-ir = { workspace = true }
rp-runtime = { features = ["serde"], workspace = true }
rp-staking = { workspace = true }
rp-state-machine = { optional = true, workspace = true }
rp-std = { workspace = true }
rp-tracing = { workspace = true }
rp-trie = { workspace = true }
rp-weights = { workspace = true }
tt-call = { workspace = true }

aquamarine = { workspace = true }

[dev-dependencies]
Inflector = { workspace = true }
frame-system = { default-features = true, workspace = true }
pretty_assertions = { workspace = true }
rp-crypto-hashing = { default-features = true, workspace = true }
rp-timestamp = { default-features = true, workspace = true }

[features]
default = ["std"]
std = [
	"binary-merkle-tree/std",
	"codec/std",
	"environmental/std",
	"frame-metadata/std",
	"frame-support-procedural/std",
	"k256/std",
	"log/std",
	"scale-info/std",
	"serde/std",
	"serde_json/std",
	"rp-api/std",
	"rp-arithmetic/std",
	"rp-core/std",
	"rp-debug-derive/std",
	"rp-genesis-builder/std",
	"rp-inherents/std",
	"rp-io/std",
	"rp-metadata-ir/std",
	"rp-runtime/std",
	"rp-staking/std",
	"rp-state-machine/std",
	"rp-std/std",
	"rp-tracing/std",
	"rp-trie/std",
	"rp-weights/std",
]
runtime-benchmarks = [
	"frame-system/runtime-benchmarks",
	"rp-runtime/runtime-benchmarks",
	"rp-staking/runtime-benchmarks",
]
try-runtime = [
	"frame-system/try-runtime",
	"rp-runtime/try-runtime",
]
experimental = ["frame-support-procedural/experimental"]
# By default some types have documentation, `no-metadata-docs` allows to reduce the documentation
# in the metadata.
no-metadata-docs = [
	"frame-support-procedural/no-metadata-docs",
	"rp-api/no-metadata-docs",
]
# By default some types have documentation, `full-metadata-docs` allows to add documentation to
# more types in the metadata.
full-metadata-docs = ["scale-info/docs"]
# Generate impl-trait for tuples with the given number of tuples. Will be needed as the number of
# pallets in a runtime grows. Does increase the compile time!
tuples-96 = ["frame-support-procedural/tuples-96"]
tuples-128 = ["frame-support-procedural/tuples-128"]
