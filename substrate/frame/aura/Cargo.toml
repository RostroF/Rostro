[package]
name = "pallet-aura"
version = "45.0.0"
authors.workspace = true
edition.workspace = true
license = "Apache-2.0"
homepage.workspace = true
repository.workspace = true
description = "FRAME AURA consensus pallet"
readme = "README.md"

[lints]
workspace = true

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
codec = { features = ["derive", "max-encoded-len"], workspace = true }
frame-support = { workspace = true }
frame-system = { workspace = true }
log = { workspace = true }
pallet-timestamp = { workspace = true }
scale-info = { features = ["derive"], workspace = true }
rp-application-crypto = { workspace = true }
rp-consensus-aura = { workspace = true }
rp-runtime = { workspace = true }

[dev-dependencies]
rp-core = { workspace = true, default-features = false }
rp-io = { default-features = true, workspace = true }

[features]
default = ["std"]
std = [
	"codec/std",
	"frame-support/std",
	"frame-system/std",
	"log/std",
	"pallet-timestamp/std",
	"scale-info/std",
	"rp-application-crypto/std",
	"rp-consensus-aura/std",
	"rp-core/std",
	"rp-runtime/std",
]
try-runtime = [
	"frame-support/try-runtime",
	"frame-system/try-runtime",
	"pallet-timestamp/try-runtime",
	"rp-runtime/try-runtime",
]
